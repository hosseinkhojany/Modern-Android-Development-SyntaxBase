<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.modern_android_development">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
    android:theme="@style/Theme.NavigationTest.NoActionBar">
<!--        <activity-->
<!--            android:name=".TextActivity"-->
<!--            android:label="@string/title_activity_text"-->
<!--            android:theme="@style/Theme.ModernAndroidDevelopment.NoActionBar"></activity>-->
        <activity
            android:name=".NavHostActivityWithXml"
            android:label="@string/app_name"
            android:theme="@style/Theme.NavigationTest.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <nav-graph android:value="@navigation/main_container" />
        </activity>

    </application>

</manifest>